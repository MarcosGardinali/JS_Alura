// == (Comparação implícita) Compara apenas os valores das variáveis;

const numero = 5;
const texto = "5";

console.log(numero == texto);

// == (Comparação explícita) Compara os valores e os tipos de dados das variáveis ;

console.log(numero === texto);